I"Ä(function() {
  $(function() {
    $('.new_comment').on('submit', function(e) {
      var target;
      e.preventDefault;
      target = $(e.target);
      console.log(target);
      $.ajax({
        url: '/comments',
        method: 'post',
        dataType: 'json',
        data: target.serialize()
      }).done(function(data) {
        var content;
        content = "<div class='title-wrapper nested'><p class='title'>" + data.body + "</p></div>";
        return target.next().append(content);
      }).fail(function(error) {
        return console.log(error);
      });
      return false;
    });
    return $('.open-nested-form').on('click', function(e) {
      var target;
      e.preventDefault;
      target = $(e.target);
      if (target.next().hasClass('hidden')) {
        target.next().removeClass('hidden');
        return false;
      } else {
        target.next().addClass('hidden');
        return false;
      }
    });
  });

}).call(this);
:ET